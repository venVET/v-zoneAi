V-TRADE AI — FINAL OLLAMA VISION SETUP

1. Install Ollama on Windows.
2. Run START-OLLAMA-VISION-BRIDGE.cmd.
   - It checks Ollama.
   - It pulls qwen2.5vl:3b if missing.
   - It starts the local bridge at http://127.0.0.1:11435.
3. Keep that CMD window open.
4. Open the V TRADE AI GitHub Pages dashboard.
5. Screenshot AI -> Choose File -> upload PNG/JPG/WebP (max 6 MB).
6. Click Analyze Screenshot.

WHY THE BRIDGE EXISTS
The browser must not call Ollama's 11434 API directly. Direct browser access can fail because of CORS.
The local bridge receives the image from the browser and calls Ollama server-side.
So the flow is:
GitHub Pages -> localhost:11435 bridge -> localhost:11434 Ollama -> Vision model.

RENDER NOTE
Render cannot reach Ollama running on your personal PC at 127.0.0.1.
Therefore the primary/local path is the PC bridge. The Render path works only if OLLAMA_BASE_URL on Render points to a reachable Ollama host. Otherwise the browser falls back to the PC bridge.

VISION SAFETY
The Vision module is evidence extraction only. It returns WAIT as the final signal and does not create orders, Entry, SL or TP.

IMPORTANT FIX v9: GitHub Pages does not proxy your PC Ollama. The browser must reach the local bridge at 127.0.0.1:11435. If Analyze shows "Bridge not reachable", start START-OLLAMA-VISION-BRIDGE.cmd on the same PC, confirm /health returns success=true, and keep the CMD window open. The GitHub Pages CSP now explicitly permits the local bridge ports.


FIX10 — Chrome Private Network Access
The local bridge includes Access-Control-Allow-Private-Network: true for GitHub Pages -> localhost preflight requests. If Chrome asks to allow local/private network access, allow it.
