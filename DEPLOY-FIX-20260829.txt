V-TRADE AI OLLAMA VISION FIX9

Fixes:
- GitHub Pages CSP explicitly allows http://127.0.0.1:11435 and http://localhost:11435.
- Screenshot Vision probes both local bridge hostnames.
- Local bridge health check has timeout and clearer diagnostics.
- Startup CMD launches bridge and immediately checks /health.
- Render Vision remains disabled by default because Render cannot reach Ollama on the user PC.

Required local flow:
GitHub Pages -> local browser bridge :11435 -> Ollama :11434 -> qwen2.5vl:3b
